{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "drift-sentinel.serviceAccountName" . }}
  labels:
    app.kubernetes.io/name: {{ include "drift-sentinel.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
{{- end }}
